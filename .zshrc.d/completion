# check for bash version

# if bash version > 3

# also approved for zsh, i think. ~basi

. ~/.bash_completion.d/git_completion
. ~/.bash_completion.d/dfm_completion

# k8s kubectl completion autogeneration. ~basi
source <(kubectl completion zsh)

# pip completion. ~basi
eval "`pip completion --zsh`"
compctl -K _pip_completion pip3
